시간초과가 발생함
import sys
input = sys.stdin.readline
이걸 추가해줘서 시간문제를 해결했는데 좀 당황스럽다.
알고리즘 문제가 아니라 읽기때문에 문제가 발생한다는게 좀 짜증난다.

그리고 까먹은 문법들
dict 에서 키가 있는지 없는지 확인을 해야할때
try except 구문으로 했는데 이렇게 하면 시간이 오래걸림
element in set 방식으로 조회 가능
len(list(dict.keys()))==1 이런방식을 사용했는데
len(dict) == 1 로도 간단하게 가능

+이걸 트리로 풀수도 있지만 트리로 푸는 것은 적절하지 않다.
정렬하고 접두사 확인이 유리하다.
